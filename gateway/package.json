{
	"name": "@checkpoint/gateway",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"build": "vite build",
		"preview": "vite build && vite preview",
		"types": "wrangler types --env-interface GatewayEnv -c wrangler.jsonc -c ../services/identity/wrangler.jsonc -c ../services/passport/wrangler.jsonc"
	},
	"devDependencies": {
		"@hono/zod-openapi": "^1.0.0-beta.1",
		"hono": "^4.8.3"
	}
}
