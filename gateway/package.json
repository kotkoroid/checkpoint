{
	"name": "@minerva/gateway",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"build": "vite build",
		"preview": "vite build && vite preview",
		"types": "wrangler types --env-interface GatewayEnv -c wrangler.jsonc -c ../services/auth/wrangler.jsonc",
	},
	"devDependencies": {
		"hono": "^4.7.10"
	}
}
