{
	"name": "@minerva/auth",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"types": "wrangler types --env-interface AuthEnv",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "wrangler d1 migrations apply auth-d1"
	}
}
