{
	"name": "@checkpoint/identity",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite serve",
		"types": "wrangler types --env-interface IdentityEnv",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "wrangler d1 migrations apply checkpoint-identity --local --persist-to ../../.wrangler/state"
	}
}
